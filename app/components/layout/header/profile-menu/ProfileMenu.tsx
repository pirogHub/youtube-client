import { FC } from 'react'

import styles from './ProfileMenu.module.scss'

const ProfileMenu: FC = () => {
	return <div className={styles.ProfileMenu}>ProfileMenu</div>
}

export default ProfileMenu
